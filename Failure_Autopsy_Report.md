What failed? (The model, the data, or the process?)
The Data failed, because the model was trained on old date and when the concept drift happened, the model was not updated.
What was the business impact? (If we trusted the AI, what happens?)
It was a disaster and a heavy losses
What is the Governance Control? (How do we prevent this next time?)
The model is trained on the Conceprt drift whenever there is an upgarade and also make sure there are always human-in-loop.
